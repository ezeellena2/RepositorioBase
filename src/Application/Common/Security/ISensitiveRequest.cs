namespace CleanArchitecture.Application.Common.Security;

/// <summary>Marks a request that carries secrets and must only use centralized no-payload logging.</summary>
public interface ISensitiveRequest
{
}
