change: account-setup-onboarding
project: repositoriobase
phase: apply
artifact_store: hybrid
session_preflight:
  pace: auto
  artifacts: hybrid
  delivery_strategy: ask-on-risk
  chain_strategy: stacked-to-main
preproposal_revision: 3
artifacts:
  explore: engram-only
  research: not_selected
  preproposal: true
  proposal: true
  specs: true
  design: true
  tasks: true
  apply_progress: true
  verify_report: false
  archive_report: false
gates:
  propose: pass on 2026-09-15 (hybrid bytes equal, 5130)
  spec: >-
    pass on 2026-09-15 (20 requirements, 75 scenarios; the sdd-spec 650-word budget is exceeded because MODIFIED
    blocks reproduce whole baseline requirements and every population needs coverage). Amended on 2026-09-15 with the
    design's W4 wording for the scenario "A signed-in caller submits a claimed CUIT"; hybrid bytes equal, 47651.
  design: >-
    PASS on 2026-09-15 after the single permitted corrective re-run (hybrid bytes equal, 45579). History: first
    validation PASS with six warnings, first corrective pass, re-validation FAIL on CRITICAL C1 (journeys not run on
    every slice), corrective re-run, final validation PASS. Follow-ups W-A, W-B and S-a to S-e were applied by sdd-tasks.
  tasks: >-
    PASS on 2026-09-15 (hybrid bytes equal, 46253; 16 slices, 139 tasks, 69 RED; the four forecast guard lines are
    present; the sdd-tasks 530-word budget is exceeded, as allowed).
  apply_slice_1: >-
    PASS on 2026-09-15. Writer verification V1 (Vitest 634/634, ESLint 0, build 0), V2, V4 (31/31 journeys) and V5;
    147 changed lines outside the change folder; review assess medium; parent spot check 24/24; fresh-context contract
    validation PASS; tasks and apply-progress hybrid bytes equal.
  apply_slice_2a_0: >-
    PASS on 2026-09-15. Writer verification focused 35/35, V1 (Vitest 634/634, ESLint 0, build 0), V2, V3
    (Application.FunctionalTests 755/755), V4 (31/31 journeys) and V5; 86 changed lines, test files only; review assess
    medium; parent spot check 35/35; fresh-context contract validation PASS; hybrid bytes equal.
decisions:
  delivery_cut: >-
    The user answered "Corte: 16 cortes" on 2026-09-15. Apply the 16 slices in tasks.md order, one verified commit per
    slice; use the planned fallback cut when a slice measures above 400 changed lines, and stop to ask for a size
    exception if a part still exceeds 400. The planning package is committed first in its own docs(openspec) commit,
    and only that documentation commit carries an approved size exception.
  slice_2a_i_cut: >-
    Slice 2a-i uses its planned fallback cut from the start (2a-i-a = tasks 3.1-3.2, then 2a-i-b = tasks 3.3-3.18),
    because its upper estimate is 510 lines and the two parts touch the same files, so they cannot be separated after
    the fact.
apply_followups:
  - Journeys (V4) and the Aspire-bearing .NET projects need Docker Desktop running.
  - Slice 3a's fallback cut keeps 3a-i and 3a-ii tests in one file (SetupRequiredTests.cs); if the cut is used, keep each commit's staged tests coherent without interactive staging.
  - Apply-progress uses the full strict-tdd.md TDD Cycle Evidence columns and names the V3 filter.
archive_followups:
  - The identity-access baseline Purpose and Current boundaries paragraphs still say account setup is absent; archive updates them.
deliveries:
  - 'Planning package: commit 2fbf313a "docs(openspec): plan account-setup-onboarding", pushed to origin/main on 2026-09-15.'
  - 'Slice 1: commit 5fac8928 "feat(identity): ask for the CUIT or DNI first", pushed to origin/main on 2026-09-15.'
  - 'Slice 2a-0: commit "test(identity): seed pre-change signed-in registrations directly" on 2026-09-15.'
tasks_progress:
  completed: [1, 2a-0]
  pending: [2a-i, 2a-ii, 2b, 3a, 3b, 4, 5a-i, 5a-ii, 5b, 6a-i, 6a-ii, 6b, 7a, 7b]
last_updated: 2026-09-15