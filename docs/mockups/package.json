{
  "name": "mockups",
  "private": true,
  "description": "Sistema de ejemplo (React + Node) que muestra cómo se ve y cómo funciona el producto. No es el proyecto real.",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "typecheck": "npm run typecheck -w api && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
