{
  "name": "mockups",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
